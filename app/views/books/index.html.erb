<!-- %h1 Book#index -->
<h1>Book#inde</h1>
<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>#</th>
  </tr>

  <% @books.each do |book| %>
  <tr>
    <td><%= book.name %></td>
    <td><%= book.desc %></td>
    <td><%= link_to 'Show', book_path(book) %></td>
  </tr>
  <% end %>
</table>
