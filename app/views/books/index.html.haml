.container
  %h1 Book List 
  -# %table
  -#   %tr
  -#     %th Name
  -#     %th Description
  -#     %th #
  -#   - @books.each do |book|
  -#     %tr
  -#       %td= book.name
  -#       %td= book.desc
  -#       %td= link_to 'Show', book_path(book)

    /= link_to "Add New Book", new_book_path, class: "btn btn-xl btn-primary", style: "color: #fff"
  %ul#books
    = render @books
  %h3 Add new book
  = render 'form'
